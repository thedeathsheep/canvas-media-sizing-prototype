# Design QA：桌面画布媒体展示尺寸

## 验收对象

- 视觉基准：`C:\Users\whatk\.codex\generated_images\019f6ac6-a6c6-7be2-975a-05f40db2fe5f\exec-29235a43-2aa6-42ac-8cb1-3f4c2f11ca51.png`
- 实现页面：`http://127.0.0.1:4178/`
- 完整对比证据：`qa/design-comparison-final.png`
- 固定节点局部对比：`qa/focus-comparison-fixed-nodes-final.png`
- 汇总评审图：`qa/design-qa-combined-final.png`
- 原始布局实现截图：`qa/raw-layout-desktop-final.jpg`
- 原始布局聚焦对比：`qa/raw-layout-comparison-final.png`

## 验收环境与状态

- 桌面端目标画面：1487 × 1058。
- 初始状态：并排对比、100% 画布缩放、信息面板收起、主视频节点选中。
- 局部状态：仅舒适展示、62% 画布缩放，平移到 16px 文本和固定流程节点。
- 布局修正状态：仅原始像素、25% 画布缩放，检查主素材、固定节点和下一媒体行之间的间距。
- 真实媒体资源使用 `object-fit: contain`，无拉伸、无占位图。

## 视觉检查

| 检查项 | 结果 |
| --- | --- |
| 双画布层级与中性深色基调 | 通过 |
| 原始/展示尺寸标签与规则说明 | 通过 |
| 图片和视频保持源宽高比 | 通过 |
| 16px 文本节点与固定流程节点可辨识 | 通过 |
| 文本、流程节点在两种模式中尺寸一致 | 通过 |
| 选中态、边框、圆角、间距与图像清晰度 | 通过 |
| 无媒体拉伸、文本裁切或关键控件遮挡 | 通过 |
| 原始像素模式 6 个节点的渲染包围盒无碰撞 | 通过 |

## 迭代记录

1. 首轮发现靠近分隔线的文本节点被裁切，属于 P2 可读性问题。调整初始相机与固定节点锚点后，文本和流程节点完整显示。
2. 第二轮发现流程节点的大面积青色主按钮抢夺文本和媒体对比的视觉层级，属于 P2 层级问题。改为紧凑的两步状态行，并在局部对比图中复核通过。
3. 第三轮发现原始像素模式复用了舒适展示模式的 `x/y`，媒体恢复到 1920～4096px 后产生节点重叠，属于 P1 核心布局问题。新增模式独立布局：主素材、固定节点、其余媒体按行排列并保留 80px 间距；组件测试与浏览器真实包围盒检查均确认 6 个节点零碰撞，聚焦对比图复核通过。
4. 未发现 P0、P1 或遗留 P2 问题。窄屏提示属于 P3 辅助信息，不影响桌面端核心验证。

## 交互检查

- 并排、仅原始像素、仅舒适展示切换正常。
- 放大、缩小、重置正常，两侧相机始终同步。
- 拖动画布后两侧位移保持一致。
- 选择任一节点后，两侧选中态与信息面板同步。
- 信息面板正确显示源尺寸、展示尺寸和展示比例。
- 原始像素模式缩放到 25% 并平移浏览后，各节点保持独立边界，无意外遮挡。
- 浏览器日志中无 error、warning 或 warn。

## 自动验证

- 12 项尺寸与相机单元测试通过。
- 4 项组件测试通过，其中包含原始布局两两碰撞回归测试。
- 生产构建通过。

final result: passed
